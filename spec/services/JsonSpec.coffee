#describe "json service", (done)->
#
#  describe "provides the mormalize function that", ->
#    it "as per [SRJ-00001] throws an DevError if no arguments were passed", ->
#      expect(json.normalize).to.throw(DevError, "No arguments passed to the normalize function" )

